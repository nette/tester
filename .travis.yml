language: php
php:
    - 5.3.3
    - 5.4
    - 5.5
    - hhvm

matrix:
    allow_failures:
        - php: hhvm
    exclude:
        - php: hhvm
    include:
        - php: hhvm
          script: "Tester/tester -p php tests/"

script: "Tester/tester tests/"
